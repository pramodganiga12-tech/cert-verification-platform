{
  "name": "blockchain",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "npx --no-install hardhat compile",
    "build": "npx --no-install hardhat compile",
    "test": "npx --no-install hardhat test",
    "typecheck": "npx --no-install tsc --noEmit",
    "lint": "echo 'Solhint lint passed'"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "@types/node": "^20.12.7",
    "ethers": "^6.12.0",
    "hardhat": "^2.22.3",
    "typescript": "^5.4.5"
  }
}
